# Post Views

一款轻量级 WordPress 文章浏览量统计插件。

## 页面预览

![页面预览](/post-view-preview.png)

## 功能特性

- 自动记录文章/页面的浏览次数
- **9 种统一胶囊形状的显示样式**（现代卡片、渐变徽章、极简线条、统计盒子、毛玻璃卡片、描边芯片、霓虹胶囊、日落胶囊、暗色实心胶囊）
- **内容类型勾选**，自由选择在哪些类型上显示浏览量
- **智能定位显示**，浏览量自动定位到内容容器右下角

## 安装

1. 下载 `post-views.zip`
2. 在 WordPress 后台 → 插件 → 安装插件 → 上传插件
3. 上传 zip 文件并启用

## 使用方法

### 自动显示

插件启用后会自动在文章/页面底部显示浏览量卡片，无需额外配置。

### 模板标签

在主题模板中手动调用：

```php
<?php if ( function_exists( 'the_views' ) ) { the_views(); } ?>
```

### 短代码

在文章或页面编辑器中插入：

```
[views]
```

指定文章 ID：

```
[views id="123"]
```

### 前端排序

通过 URL 参数按浏览量排序：

```
?v_sortby=views&v_orderby=desc
```

### 模板变量

单篇模板支持：

| 变量 | 说明 |
|------|------|
| `%VIEW_COUNT%` | 格式化的浏览次数 |
| `%VIEW_COUNT_ROUNDED%` | 缩写形式（如 1.2K、3.5M） |

列表模板额外支持：

| 变量 | 说明 |
|------|------|
| `%POST_TITLE%` | 文章标题 |
| `%POST_URL%` | 文章链接 |
| `%POST_DATE%` | 发布日期 |
| `%POST_TIME%` | 发布时间 |
| `%POST_EXCERPT%` | 文章摘要 |
| `%POST_CONTENT%` | 文章内容 |
| `%POST_THUMBNAIL%` | 缩略图 HTML |
| `%POST_THUMBNAIL_URL%` | 缩略图 URL |
| `%POST_CATEGORY_ID%` | 分类 ID |
| `%POST_AUTHOR%` | 作者名 |

## 设置

启用后在 WordPress 后台 → 设置 → 浏览量 中配置：

### 显示样式
  - **现代卡片**：紫色渐变背景 + 图标 + 悬停上浮动画
  - **渐变徽章**：粉紫渐变胶囊 + 图标 + 脉动动画（默认）
  - **极简线条**：透明背景 + 灰色描边胶囊
  - **统计盒子**：白底彩色描边胶囊 + 图表图标
  - **毛玻璃卡片**：半透明背景 + 毛玻璃效果 + 深度阴影
  - **描边芯片**：透明背景 + 彩色边框 + 悬停填充
  - **霓虹胶囊**：深蓝黑底 + 青色发光边框/文字
  - **日落胶囊**：橙黄渐变胶囊
  - **暗色实心胶囊**：深灰底 + 绿色呼吸点

### 内容类型
- 勾选要显示浏览量的文章类型
- 支持所有公开类型：文章（post）、页面（page）、自定义类型等
- 默认启用文章和页面

### 基础设置
- 统计来源（所有人 / 仅游客 / 仅注册用户）
- 是否排除爬虫
- 是否使用 AJAX 更新（缓存环境下）
- 浏览量显示模板（高级自定义）
- 热门列表模板
- 各页面类型的显示规则

## 随机写入浏览数据（测试功能）

在设置页面底部可为所有已发布文章随机写入浏览数据：

- 执行时仅重置「已发布的公开文章」的浏览量为 0，再为每篇文章随机写入（不会误清其他 postmeta 或草稿/私密内容）
- 默认随机范围：100 ~ 1000
- 支持自定义最小值和最大值

> 注意：该功能为测试功能，执行后原有浏览数据将被覆盖且不可恢复。

## 暗色模式

插件自动适配暗色模式，支持：

- 系统级：`prefers-color-scheme: dark`
- 主题级：`body.dark`、`body.dark-mode`、`body.night-mode`、`html.dark`、`[data-theme="dark"]`、`[data-color-scheme="dark"]`

## 第三方主题适配（JustNews 等）

不少主题（如 JustNews）在后台提供「文章信息 → 阅读数」开关，并注明"需安装 WP-Postviews 插件"。这类主题通常不调用插件自身的输出函数，而是直接读取 `views` 数据，本插件已原生兼容：

- **自动识别**：主题通过 `function_exists('the_views')` 探测插件是否启用，本插件提供 `the_views()`，探测成功。
- **选项兼容**：主题调用 `get_option('views_options')`，本插件沿用同名选项，读取正常。
- **显隐判断**：主题调用 `should_views_be_displayed( $views_options )`，本插件提供同名全局函数，按各页面类型的显示规则返回结果。
- **列表卡片数字**：主题读取 `$post->views`，本插件通过 `the_posts` 过滤器批量填充，卡片上不会显示为 0。

> 提示：启用后到主题后台勾选「阅读数」开关即可。若已勾选但仍不显示，请检查本插件「设置 → 内容类型」中是否勾选了对应文章类型，并清除缓存后重试。

### 快讯（kuaixun）详情页

JustNews 的快讯单页模板（`single-kuaixun.php`）通过 `the_excerpt` 输出内容，**不走** `the_content`，因此常规的"文章底部自动追加浏览量"不会生效。若需在快讯详情页显示阅读数，请在本插件「设置 → 基础设置 → 快讯详情页显示浏览量」中开启。开启后会在快讯内容末尾追加一个轻量标签（眼睛图标 + 数字），同时该类型也参与计数，确保数字随访问增长。仅作用于快讯详情页，不影响列表卡片与其他页面。

## 兼容性

- WordPress 6.0+
- PHP 7.4+
- 兼容主流缓存插件（WP Super Cache、W3 Total Cache、LiteSpeed Cache 等）
- 适配 JustNews 等第三方主题：自动兼容原版 WP-Postviews 的 `should_views_be_displayed()` 接口与 `$post->views` 属性读取

## 更新日志

### 2.0.10
- 修复 v2.0.9 快讯详情页标签不显示的问题：实测发现 `the_excerpt` 过滤器在生产环境的 JustNews 主题下未能触发（具体原因涉及主题层 hook 处理），改用前端 DOM 注入：开启「快讯详情页显示浏览量」后，会在快讯详情页加载一段小 JS，自动在内容区后追加一个 inline 浏览量标签
- 新增 `postviews-kuaixun.js` 前端脚本 + `maybe_enqueue_kuaixun_injector()` 调度方法
- 彻底移除 v2.0.9 的 `the_excerpt` 过滤器及相关调试代码

### 2.0.9
- 新增「快讯详情页显示浏览量」开关（早期实现，v2.0.10 替换为 DOM 注入）

### 2.0.8
- 新增第三方主题兼容层（JustNews 等）：
  - 恢复全局函数 `should_views_be_displayed( $options )`，兼容原版 WP-Postviews 的调用签名
  - 新增 `the_posts` 过滤器，批量填充 `$post->views`，修复列表卡片阅读数恒为 0 的问题
- 新增 `tests/test-compat.php`，覆盖兼容层行为

### 2.0.0
- 9 种全新胶囊样式，视觉一致
- 后台样式预览改为加载前端 CSS，预览与前台显示完全一致
- AJAX 计数增加基于 IP + 文章的去重防刷（3 秒冷却窗口）
- 随机写入功能收窄范围：仅重置已发布的公开文章，避免误清其他 meta
- 热门/冷门列表查询与总浏览量查询增加缓存（1 小时），降低数据库压力

## 作者

木木 — [blog.huzz.cn](https://blog.huzz.cn/)

## 许可证

GPLv2 or later
